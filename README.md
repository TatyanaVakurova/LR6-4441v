# LR6
Лабораторная работа №6

Система контроля версий

Работу  выполнил: студент группы 4441в Вакурова Татьяна Александровна

**Цель работы:** *изучение базовых возможностей системы
управления версиями, опыт работы с Git Api, опыт работы с локальным и
удаленным репозиторием.*

**1. Сделана копия в личное хранилище из https://github.com/Kurtyanik/LR6/ (Fork)** (см. форк репозитория.png)

**2. Установлен Git (https://git-scm.com/).**

**3. После установки настроен клиент Git**

    git config --global user.name "4441в Vakurova Tatyana"
    git config --global user.email "vakurovatatyana@mail.ru"
**4. Клонирование репозитория на компьютер** (см. клонирование.png)

    git clone https://github.com/TatyanaVakurova/LR-4441v
**5. Добавлен файл, подтянуты изменения в репозиторий** (см. создан файл.png)

    touch new_file.txt
    git pull origin master
**6. Получена история операций для каждой из веток** (см. просмотр веток.png)

    git log --all --decorate --oneline --graph
**7. Просмотрены последние изменения** (см. git log.png)
    
    git log
**8. Создание новой ветки** (см. слияние веток.png)

    git checkout -b feature/new-feature
   
**9. Выполнено слияниие в ветку master, разрешив конфликт(убрать символы).** (см. слияние веток.png и конфликт.png)

    git checkout master
    git merge -b feature/new-feature
**10. Удалена побочная ветка после успешного слияния** (см. удаление ветки.png и конфликт исправлен.png)

    git branch -d feature/new-feature
Также для корректного отображение удаленную ветку нужно удалить с github (см. удаление ветки с гит.png)

    git push origin --delete feature/new-feature 
**11. Сделаны изменения и зафиксированы, оставлены комментарии. Также сделан откат коммита** (см. откат изменений коммита.png)

    echo "Another update" >> new_file.txt
    git add new_file.txt
    git commit -m "добавили строчку"
    git resert HEAD~1
**12. Создана ветка для отчета (report)** (см. новая ветка для отчета.png)

    git checkout -b report
**13. Получена история операций в форматированном виде.**

**14. На GitHub в отдельную папку загружены снимки экрана, сделаные в ходе работы.**

**15. Вывод:** *В ходе выполнения работы я повторил базовые возможности системы управления версиями, получил опыт работы с Git Api, опыт работы с локальным и удаленным репозиторием.*
